# 簡單的 Mosquitto 配置 - 無 TLS、無密碼認證
persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
log_type all

# MQTT 標準端口
listener 1883
protocol mqtt

# 允許匿名連接（測試用）
allow_anonymous true

# 保留消息設定
max_queued_messages 1000
message_size_limit 0

# 連接設定
max_connections -1
keepalive_interval 60
